<%- include('partials/logged-in-header') -%>
    <!-- <div class="container">
        <div class="row justify-content-center mt-5">
            <div class="col-6 postTitle">
                <h2>
                    <%= activity.name %>
                </h2>
            </div>
            <div class="row sectionOne">
                <div class="row col-md-6 leftSection">
                    <div class="postImg">
                        <img class=" img-fluid " src=" <%=activity.image%>" />
                    </div>

                    <!-- <div class="row justify-content-between"></div> -->
    <!-- <div class="row d-flex justify-content-sm-between ">
                        <form class="col-1" action="/activity/likeActivity/<%= activity.id %>?_method=PUT"
                            method="POST">
                            <button class="btn btn-primary fa fa-heart" type="submit"></button>
                        </form>

                        <form class="col-1" action="/activity/favoriteActivity/<%= activity.id %>" method="POST">
                            <button class="btn btn-primary fa fa-star" type="submit"></button>
                        </form>

                        <h3 class="col-3">Likes: <%= activity.likes %>
                        </h3>
                        <%if(activity.user==user.id){ %>
                            <form action="/activity/deleteActivity/<%= activity.id %>?_method=DELETE" method="POST"
                                class="col-3">
                                <button class="btn btn-primary fa fa-trash" type="submit"></button>
                            </form>
                            <%}%>
                    </div>
                </div>


                <section class="row col-md-6 d-flex flex-column recipeSection">
                    <div class="col-3 mt-5 p-2 postTitle">
                        <h3 class="text-center">
                            <%= activity.name %>
                        </h3>

                    </div>

                    <div class="col-3 mt-5 p-2">
                        <h4 class="text-center">Caption</h4>
                        <p class="text-start">
                            <%= activity.caption %>
                        </p>
                    </div>
                    <div class="col-3 mt-5 p-2">
                        <h4 class="text-center">Material</h4>
                        <p class="text-start">
                            <%= activity.material %>
                        </p>
                    </div>
                    <div class="col-3 mt-5 p-2">
                        <h4 class="text-center">Directions</h4>
                        <p class="text-start">
                            <%= activity.directions %>
                        </p>
                    </div>
                </section>


            </div>

        </div>


        <div class="mt-5">
            <h2>Add a comment</h2>
            <form action="/comment/createComment/<%=activity._id%>" method="POST">
                <div class="mb-3">
                    <label for="comment" class="form-label">Comment</label>
                    <input type="text" class="form-control commentForm" id="comment" name="comment">
                </div>
                <button type="submit" class="btn btn-primary">Submit</button>
            </form>
        </div>
        <ul>
            <% for(var i=0; i<comments.length; i++) {%>
                <li class="col-6 justify-content-between mt-5">
                    <%= comments[i].comment%>
                </li>
                <% } %>
        </ul>
        <div class="col-6 mt-5 d-flex justify-content-between returnSection">
            <a class="btn btn-primary p-2" href="/profile">Return to Profile</a>
            <a class="btn btn-primary p-2" href="/feedA">Return to Feed</a>
        </div>
    </div>  -->


    <!-- New One -->
    <div class="container">
        <div>
            <!-- <h1>
                <%= activity.title %>
            </h1>
            <p>
                <%= activity.caption %>
            </p>
            <p>By: <%= user.userName %>
            </p> -->
            <!-- <div class="col-md-6"></div> -->
            <div>
                <div class="p-3 max-w-md mx-auto flex justify-center">
                    <img src="<%= activity.image%>" class="rounded-lg max-w-lg max-h-full" alt="Responsive image">

                </div>

                <h1 class="display-4 activity-name">
                    <%= activity.name %>
                </h1>

                <p class=" p-3 activity-user">By: <%= user.userName %>
                </p>

                <div class="p-4">
                    <form class="inline-flex items-center px-4 py-2 text-gray-800 text-sm font-medium rounded-md"
                        action="/activity/likeActivity/<%= activity.id %>?_method=PUT" method="POST">
                        <button class=" <%if(activity.likes.includes(user.id)){ %> btn-liked <%} %>
            btn btn-primary fa fa-heart" type="submit"> Like</button>
                        <h3 class="col">
                            <%= activity.likes.length - 1 %>
                        </h3>
                    </form>

                    <form class="inline-flex items-center px-4 py-2text-gray-800 text-sm font-medium rounded-md"
                        action="/activity/favoriteActivity/<%= activity.id %>?_method=PUT" method="POST">
                        <button class=" <%if(activity.favorites.includes(user.id)){ %> fa fa-bookmark-fill<%} else { %> fa fa-bookmark <% }%>
            btn btn-primary " type="submit"> Bookmark</button>
                    </form>
                    <!-- <%if(activity.user==user.id){ %> -->
                    <form action="/activity/deleteActivity/<%= activity.id %>?_method=DELETE" method="POST"
                        class="inline-flex items-center px-4 py-2  text-gray-800 text-sm font-medium rounded-md">
                        <button class="btn btn-primary fa-solid fa-trash" type="submit"> Delete</button>
                    </form>
                    <%}%>

                </div>
            </div>

            <!-- <div class="col-md-6"></div> -->
            <div>
                <!-- <h3 class="display-4">Recipe</h3>
                <h1>
                    <%= activity.title %>
                </h1> -->
                <p class="activity-caption">
                    &ldquo;
                    <%= activity.caption %>
                        &#8221

                </p>
                <h2 class="activity-material">Material</h2>
                <hr class="style1">
                <ul class="list-unstyled">
                    <% for(var i=0; i<activity.material.length ; i++) {%>
                        <li class="p-1 g-2">
                            <%= activity.material[i] %>
                        </li>
                        <% } %>
                </ul>
                <h2 class="activity-directions">Directions</h2>
                <hr class="style1">
                <ol class="list-group list-group-numbered">
                    <% for(var i=0; i<activity.directions.length; i++) {%>
                        <li class="p-1">
                            <%= activity.directions[i] %>
                        </li>
                        <% } %>
                </ol>
            </div>
        </div>
        <div class="mt-5 comment-section">
            <h2 class="display-6">Leave a comment</h2>
            <form action="/comment/createComment/<%=activity._id%>" method="POST">
                <div class="mb-3">
                    <label for="comment" class="form-label display-7">Comments</label>
                    <input type="text" class="form-control commentForm" id="comment" name="comment">
                </div>
                <button type="submit" class="btn btn-primary">Submit</button>
            </form>
        </div>
        <ul>
            <% for(var i=0; i<comments.length; i++) {%>
                <li class="col-6 justify-content-between mt-5">
                    <%= comments[i].comment%>
                </li>
                <% } %>
        </ul>


    </div>

    <!-- End New One -->




    <div class="col-6 mt-5 d-flex justify-content-between returnSection">
        <a class="btn btn-primary p-2" href="/profile">Return to Profile</a>
        <a class="btn btn-primary p-2" href="/feedA">Return to Feed</a>
    </div>


    <%- include('partials/footer') -%>